<div class="container">
  <%= render( 'layouts/errors', :object => @style) %>

 <%= form_for [@brewery, @style] do |f| %>
   <%= f.label :style_name %>
   <%= f.text_field :style_name %>
   <%= f.label :country_of_origin %>
   <%=  f.text_field :country_of_origin%>
   <%=  f.label :description%>
   <%= f.text_field :description %>
   <br>
   <br>
   <%= f.submit %>
 <%end %>

</div>
